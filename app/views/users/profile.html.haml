.body-container--plain-row
  .body-container--content
    .row-fluid
      .span12
        = image_tag(@user.avatar_url(:medium)) if @user.avatar?

    .row-fluid
      .span6.pull-right
        = @user.full_name
      .span6
        = @user.bio

    .row-fluid
      .headline Dự Án (#{@user.projects.public_view.count})
      %hr/ 
      - @user.projects.public_view.each do |project|
        = render "projects/bar_display", :project => project

    .row-fluid
      .headline Donation History
      %hr/ 
      - @user.donations.each do |donation|
        %p
          - donation.attributes.each do |key, value|
            = link_to project_donation_path(donation.project, donation) do
              %b= key.humanize
              = value
              |